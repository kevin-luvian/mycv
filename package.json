{
  "name": "mycv",
  "version": "0.0.0",
  "private": true,
  "dependencies": { },
  "scripts": {
    "start": "cd ./backend && npm istall && node ./bin/www",
    "heroku-postbuild": "cd ./frontend && npm install --only=dev && npm install && npm run build"
  }
}
